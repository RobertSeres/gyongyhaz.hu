## 2025-04-22 - Synchronized Mobile Menu State
**Learning:** Using a unified `toggleMenu` function to manage ARIA attributes (`aria-expanded`, `aria-label`), Font Awesome icons, and visibility classes ensures a consistent experience across all trigger points (toggle button and navigation links).
**Action:** Always wrap multi-indicator UI state changes in a single management function to prevent desynchronization between visual and accessible states.
