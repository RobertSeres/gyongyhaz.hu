## 2026-04-10 - Mobile Menu and Form Accessibility
**Learning:** Screen readers and keyboard users need explicit state feedback (like `aria-expanded`) and visible focus indicators to navigate interactive elements like mobile menus effectively.
**Action:** Always synchronize ARIA attributes and visual states (e.g., icons) in a centralized JavaScript function for UI components with multiple triggers.
